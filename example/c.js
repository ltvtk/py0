// Arrow function example
add = (x, y) => x + y

// Using the arrow function
result = add(5, 3)
print(result)

// Arrow function with a single parameter
square = x => x * x

// Using the single parameter arrow function
print(square(4))

// Arrow function with no parameters
greet = () => print("Hello, world!")

greet()